Everything runs in the frontend, make sure to have all the JSON stuff in `output.json`.

# TODO:

update the website so that you can search by tags.

make default colour scheme dark, add light mode slider.
